<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- not pressed -->
    <item android:color="@color/colorBlack" android:state_selected="false" />

    <!-- pressed -->
    <item android:color="@color/colorWhite" android:state_selected="true" />

    <!-- Default color -->
    <item android:color="@color/colorgreyish_brown" />

</selector>